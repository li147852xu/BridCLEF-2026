# BirdCLEF+ 2026 — cloud training env (AutoDL RTX 5090, Ubuntu 22.04, CUDA 12.8).
#
# Install order matters: torch first (pinned to CUDA 12.8 build for Blackwell
# sm_120 kernels), then the rest. See scripts/cloud_bootstrap.sh for the
# exact install recipe.

# --- deep learning -----------------------------------------------------------
# PyTorch 2.8.0 + cu128. 2.8 is the first stable release with sm_120 (RTX 5090)
# kernels baked into the stock wheel; older wheels will JIT-compile at startup
# and waste ~5 min + VRAM per run.
torch==2.8.0
torchvision==0.23.0
torchaudio==2.8.0

# Pretrained backbones (eca_nfnet_l0, tf_efficientnetv2_s_in21k, etc.)
timm==1.0.11

# --- audio / signal ----------------------------------------------------------
soundfile>=0.12
resampy>=0.4
librosa>=0.10,<0.11           # only for a few helpers; mel-spec runs through torchaudio

# --- training utilities ------------------------------------------------------
numpy>=1.26,<2.1
pandas>=2.1,<3
pyarrow>=15
scipy>=1.11,<2
scikit-learn>=1.4,<2
tqdm>=4.66
pyyaml>=6
einops>=0.7

# --- Perch v2 (TF SavedModel; GPU path on 4090 is much faster than Mac CPU) --
# TF 2.20 is required for StableHLO ops in Perch v2.
tensorflow==2.20.0

# --- export / inference optimisation ----------------------------------------
onnx>=1.17
onnxruntime>=1.19
onnxscript>=0.1.0.dev20240926 ; python_version >= "3.10"
openvino==2024.4.0

# --- cloud sync / IO --------------------------------------------------------
huggingface_hub>=0.25
kaggle>=1.6.17

# --- dev / debug ------------------------------------------------------------
rich>=13
